<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Tesla & SpaceX Grok Mining & Stock Preview</title>
  <link rel="icon" href="https://www.tesla.com/favicon.ico" type="image/x-icon">
  <style>
    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: linear-gradient(135deg, #000814, #001233);
      color: #e0e0ff;
      min-height: 100vh;
      line-height: 1.6;
    }
    header {
      background: rgba(0,0,0,0.92);
      padding: 80px 20px 50px;
      text-align: center;
      border-bottom: 5px solid #00ff9d;
    }
    h1 {
      margin: 0;
      font-size: 3.8rem;
      color: #00ff9d;
      text-shadow: 0 0 40px #00ff9daa;
    }
    .subtitle {
      font-size: 1.8rem;
      color: #a0f0c0;
      margin: 20px 0;
    }
    .ticker {
      background: rgba(0,0,0,0.8);
      border-radius: 16px;
      padding: 30px;
      margin: 50px auto;
      max-width: 1100px;
      text-align: center;
      font-size: 1.8rem;
      font-weight: bold;
      border: 1px solid #00ff9d44;
    }
    .ticker span { color: #00ff9d; }
    .container { max-width: 1200px; margin: 60px auto; padding: 0 20px; }
    .card {
      background: rgba(10,20,40,0.85);
      border: 1px solid #00ff9d55;
      border-radius: 24px;
      padding: 50px;
      margin: 50px 0;
      backdrop-filter: blur(14px);
      box-shadow: 0 15px 60px rgba(0,0,0,0.8);
    }
    input, button {
      width: 100%;
      padding: 24px;
      margin: 24px 0;
      border-radius: 14px;
      border: none;
      font-size: 1.6rem;
    }
    input {
      background: rgba(255,255,255,0.12);
      color: white;
    }
    button {
      background: linear-gradient(90deg, #00ffd, #00cc88);
      color: black;
      font-weight: bold;
      cursor: pointer;
      transition: all 0.3s;
    }
    button:hover {
      transform: scale(1.05);
      box-shadow: 0 0 45px #00ff9d88;
    }
    #result {
      margin-top: 50px;
      padding: 40px;
      background: rgba(0,255,157,0.3);
      border-radius: 20px;
      font-size: 2.4rem;